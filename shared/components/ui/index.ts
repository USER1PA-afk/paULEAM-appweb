/**
 * Shared UI Components (Shadcn UI)
 *
 * Componentes de UI reutilizables copiados desde Shadcn UI.
 * Se agregan bajo demanda, no se instalan masivamente.
 *
 * Para agregar un nuevo componente:
 * 1. Copiar el código del componente desde ui.shadcn.com
 * 2. Crear el archivo en este directorio
 * 3. Exportar desde este barrel
 */

export {};
